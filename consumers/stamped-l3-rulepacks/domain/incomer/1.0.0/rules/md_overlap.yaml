id: md_overlap
category: md_overlap
engine: rules.md_overlap
version: 1.0.0
waste_category: 1
formula_ref: window_peak_kva = max(15min_mean_kva); flag if peak > CMD risk with co-start
defaults:
  threshold_kva: 900
  baseline_band:
  - 800.0
  - 850.0
  stagger_min: 8
suppressions:
- startup_window
- production_mix_change
- maintenance_calendar
- data_quality
